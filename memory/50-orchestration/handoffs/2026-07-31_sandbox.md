# Handoff — sandbox e adapter Git

- **from**: sandbox-adapter-engineer
- **to**: persistence-adapter-engineer
- **intent**: implement
- **context**: `packages/sandbox/src/index.ts`; `packages/adapter-git/src/index.ts`; `packages/contracts/src/index.ts`; `memory/90-decisions/0044-forja-2-sandbox-worktree.md`; `docs/architecture/FORJA-2.0-IMPACT-FOUNDATION.md`
- **acceptance**: Sandbox controla ciclo isolado, validação aceita, diff obrigatório, promoção explícita, rejeição e destruição; adapter Git traduz operações por portas e não contém regra de domínio.
- **constraints**: domínio sem Git/filesystem/subprocessos; promoção e destruição crítica devem ser autorizadas pela camada de Policy; não executar comandos reais nos testes determinísticos; manter auditoria e contratos versionados.
- **return**: implementar `packages/adapter-sqlite` para persistir contratos, sessões, eventos, checkpoints, sprints, tasks, handoffs e auditoria, com migração não destrutiva e testes de idempotência/retomada.

## Evidências

- `npm run types:check`: passou.
- `node --test test/sandbox.test.js test/adapter-git.test.js`: 2 arquivos passaram.
- `git diff --check`: passou.

## Métricas de tokens

- orçamento explícito nos fixtures: 150 tokens de entrada e 50 de saída quando aplicável;
- consumo de LLM: não aplicável; implementação, parsing e validação foram determinísticos;
- cache: não aplicável nesta unidade.
