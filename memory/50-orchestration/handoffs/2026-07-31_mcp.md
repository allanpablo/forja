# Handoff — MCP adapter

- **from**: mcp-application-engineer
- **to**: backend-application-engineer
- **intent**: implement
- **context**: `packages/mcp/src/index.ts`; `packages/core/src/index.ts`; `packages/context/src/index.ts`; `packages/graph/src/index.ts`; `packages/orchestration/src/index.ts`; `packages/adapter-sqlite/src/index.ts`; `memory/90-decisions/0046-forja-2-mcp-adapter.md`; `docs/architecture/FORJA-2.0-IMPACT-FOUNDATION.md`
- **acceptance**: tools e recursos oficiais são descobríveis; chamadas delegam aos engines; inputs inválidos e dependências ausentes geram erros estruturados; identidade e Policy são transportadas; MCP não duplica domínio.
- **constraints**: sem lógica de negócio no adapter; Registry é a única execução de capability; dados de recursos devem vir de providers; nenhuma ação mutável ignora Policy; manter operação local/offline.
- **return**: integrar `packages/adapter-nest` e `apps/server` com REST + SSE, OpenAPI, correlation IDs, autenticação local e composição dos adapters sem contaminar o domínio.

## Evidências

- `npm run types:check`: passou.
- `node --test test/mcp.test.js`: passou.
- `git diff --check`: passou.

## Métricas de tokens

- orçamento explícito nos fixtures: 150 tokens de entrada e 50 de saída quando aplicável;
- consumo de LLM: não aplicável; dispatch, validação e normalização foram determinísticos;
- cache: não aplicável nesta unidade.
