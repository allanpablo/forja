# Base de Conhecimento Compartilhada (Patterns)

> Preenchida via Cross-Project Analysis (`npm run memory:extract`): padrões que se
> repetem em 2+ projetos do workspace são promovidos para cá, **sem dados de
> produto** — apenas o padrão generalizado.

Nenhum padrão promovido ainda nesta instalação. Conhecimento específico de cada
produto vive na ficha do projeto em `<workspace>/memory/30-projects/`.
